# Use server-wide fixed roles

Server authorization will use exactly three fixed roles: one Owner, zero or more Admins, and Members. Every member automatically participates in open projects and open direct server channels; private projects and private direct server channels use explicit member lists. Members may post and invoke available Agents in every Project they can access, while Owner and Admin add management capabilities. Buzzcode will not initially support custom roles.
